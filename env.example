# Application Settings
# Required for all environments
APP_ENVIRONMENT=development
APP_CORS_ORIGINS=*

# Google Cloud Settings
# Required for Gmail integration (optional for local testing)
GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account-key.json
GMAIL_USER_EMAIL=assignments@yourdomain.com
# Required for production deployment
GCP_PROJECT_ID=your-project-id

# Pub/Sub Settings
# Required for production Gmail notifications (optional for local testing)
PUBSUB_TOPIC=gmail-notifications
PUBSUB_SUBSCRIPTION=gmail-notifications-sub

# Database Configuration
# Choose ONE of the following based on your environment:

# Development: SQLite (default)
# DATABASE_URL=sqlite:///assignments.db

# Development: Local PostgreSQL (recommended for testing migrations)
# DATABASE_URL=postgresql://username:password@localhost:5432/riv_assignments_dev

# Production: Cloud SQL (required for production deployment)
# DATABASE_URL=postgresql://username:password@/riv_assignments_prod?host=/cloudsql/project:region:instance

